순환(recursion): 어떤 알고리즘이나 함수가 자기 자신을 호출하여 문제를 해결하는 프로그래밍 기법
- 팩토리얼(n!)과 같은 순환적인 문제를 계산하는데 유용하다
- 순환을 사용하게 되면 단순하게 작성이 가능하며 가독성이 높아진다
- 일반적으로 순환은 함수 호출을 하게 되므로 반복에 비해 수행속도가 늦다. 하지만 빠른 경우도 있음(ex.거듭제곱)
	
반복(iteration): for나 while 등의 반복구조로 되풀이하는 것이다
- 반복은 간명하고 효율적으로 되풀이를 구현하는 방법이다


